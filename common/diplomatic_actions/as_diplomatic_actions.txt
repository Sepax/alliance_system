allianceaction = {
	condition = {
		tooltip = NOT_ENOUGH_POINTS
		potential = {
			ai = no
		}
		allow = {
			OR = {
				NOT = {
					has_country_flag = too_many_alliances
				}
				FROM = {
					NOT = {
						has_country_flag = too_many_alliances
					}
				}
			}
		}
	}
	condition = {
		tooltip = NOT_ENOUGH_POINTS
		potential = {
			has_country_flag = too_many_alliances
		}
		allow = {
			NOT = {
			   has_country_flag = too_many_alliances
			}
		}
	}

	condition = {
		tooltip = FROM_NOT_ENOUGH_POINTS
		potential = {
			FROM = {
				has_country_flag = too_many_alliances
			}
		}
		allow = {
			NOT = {
				FROM = {
					has_country_flag = too_many_alliances
				}
			}
		}
	}
}
